# /status - 查看项目状态

显示当前项目的工作状态，包括活动变更、任务进度、阻塞项和下一步行动。

## 用法

```bash
npx tsx commands/status.ts
```

## 你需要报告的内容

### 1. 检查环境

1. 验证 `context/` 目录存在（product.md, tech-stack.md, workflow.md）
2. 如果不存在，提示："项目未设置。请运行 `/setup` 初始化。"

### 2. 读取状态

1. 读取 `.fusion/status.json` 获取活动变更
2. 扫描 `changes/` 目录获取可用变更列表

### 3. 解析任务进度

对于活动变更，读取 `changes/{name}/tasks.md`：

- 统计 `- [x]` 已完成任务
- 统计 `- [ ]` 待完成任务  
- 统计 `- [/]` 进行中任务
- 检测 `<!-- blocked -->` 标记的阻塞项

### 4. 生成状态报告

输出格式：

```
╭─────────────────────────────────────────╮
│           📊 Project Status             │
╰─────────────────────────────────────────╯

🔧 Active Change: {name}
   Started: {date}
   Progress: [████████░░░░░░░░░░░░] 40%
   Tasks: 4/10 completed, 6 pending

⚠️ Blockers:
   - {blocker description}

📋 Next Action:
   Execute task: "{next uncompleted task description}"

📁 Available Changes:
  → {active}
    {other1}
    {other2}
```

### 5. 下一步提示

根据状态提供下一步建议：

- 如果有未完成任务："使用 `/implement` 继续执行任务"
- 如果所有任务完成："使用 `/archive {name}` 归档此变更"
- 如果无活动变更："使用 `/new-change {name}` 创建新变更"
