# /implement - 选择变更开始实现

选择一个变更（change）并准备执行其任务。

## 用法

```bash
# 使用活动变更
npx tsx commands/implement.ts

# 指定变更名称
npx tsx commands/implement.ts my-feature
```

## 功能

1. **列出可用变更**：显示 `changes/` 目录下的所有变更
2. **选择目标变更**：自动使用活动变更或指定的变更
3. **显示任务进度**：统计 `tasks.md` 中的完成情况
4. **初始化状态**：如果变更未被跟踪，自动初始化

## 输出示例

```
╭─────────────────────────────────────────╮
│           🚀 Implement                  │
╰─────────────────────────────────────────╯

📁 Available Changes:
  → my-feature
    another-change

🔧 Selected: my-feature
   Started: 2026-01-02 18:00:00
   Progress: [████████░░░░░░░░░░░░] 40%
   Tasks: 4/10 completed, 6 pending

📋 Next Step:
   Use /execute-plan to start implementing tasks for this change.
```

## 下一步

选择变更后，使用 `/execute-plan` 开始逐个执行任务。
